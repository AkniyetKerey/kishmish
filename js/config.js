// ============================================================
// config.js — все константы проекта Kish-Mish в одном месте.
// Меняешь цену контейнера, номер WhatsApp или список точек — только здесь.
// ============================================================

// Цена одного контейнера (тг), добавляется за каждое блюдо в заказе
const CONTAINER_PRICE = 80;

// Номер WhatsApp, куда улетает готовый заказ
const WHATSAPP_PHONE = '77759321308';

// Доступные филиалы/точки (используются на экране выбора филиала)
const BRANCHES = ['27-71', '28-63', '17-95'];

// Ключевые слова для блока "🔥 В тренде" — ищутся в названии блюда
const TRENDING_KEYWORDS = ['ташкентский', 'гуйру', 'комбо #3', 'шашлык ассорти'];

// Ключи localStorage
const STORAGE_KEYS = {
    state: 'kishMishState',   // корзина, тема, тип заказа, адрес и т.п.
    branch: 'kishMishBranch'  // выбранный филиал
};

// Стоимость доставки и порог, при котором она становится дешевле
const DELIVERY_COST = {
    standard: 800,
    discounted: 500,
    discountThreshold: 5000
};

// Диапазон суммы заказа, при котором показываем подсказку "добавь ещё немного"
const UPSELL_RANGE = { min: 3500, max: 5000 };
